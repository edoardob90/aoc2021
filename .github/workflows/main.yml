name: Update calendar on README
on:
  schedule:
    - cron: '27 21 * * *'
jobs:
  update-calendar:
    runs-on: ubuntu-latest
    steps:
      - name: Run shell script
        run: ./.github/scripts/update_calendar.sh
        shell: bash
      - run: echo "🍏 This job's status is ${{ job.status }}."

